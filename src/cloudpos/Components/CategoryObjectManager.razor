<h3 class="fw-bold mt-3">카테고리 관리</h3>
    <h6 class="text-secondary fw-light">총 @(Categories.Count)개의 카테고리가 있습니다.</h6>
    <div class="d-flex flex-column gap-3 my-4">
        <div class="card shadow p-3 d-flex flex-column justify-content-center align-items-center text-center bg-body-tertiary" @onclick="AddCategoryAsync">
            <i class="bi bi-plus-lg" style="font-size: 2rem;"></i>
            <p class="mb-0">새 카테고리 추가</p>
        </div>
        <Virtualize Items="Categories" Context="category">
            <div class="card d-flex flex-row shadow py-3 px-4 bg-body-tertiary">
                <div class="flex-grow-1">
                    <p class="monospace-font">#@category.CategoryId</p>
                    <h6 class="fw-bold">@category.Name</h6>
                    @if (category.Items.Count > 0)
                    {
                        <p class="mb-0">이 카테고리에 @(category.Items.Count)개의 메뉴가 있습니다.</p>
                    }
                    else
                    {
                        <p class="mb-0">이 카테고리에 아무 하위 메뉴가 없습니다.</p>
                    }
                </div>
                <div class="d-flex justify-content-between flex-column">
                    <button class="btn btn-outline-primary border-0" @onclick="() => EditCategoryAsync(category)">
                        <i class="bi bi-pencil-fill"></i>
                        수정
                    </button>
                    <button class="btn btn-outline-danger border-0"  @onclick="() => DeleteCategoryAsync(category)">
                        <i class="bi bi-x-lg"></i>
                        삭제
                    </button>
                </div>
            </div>
        </Virtualize>
    </div>
